{
  "name": "ekagra",
  "private": true,
  "packageManager": "bun@1.3.14",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "lint": "biome check .",
    "test": "bun test $(rg --files packages apps/cli apps/mobile firmware supabase -g '*.test.ts' -g '*.test.tsx')",
    "typecheck": "tsc --noEmit && tsc --noEmit -p apps/mobile/tsconfig.json"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.0",
    "@types/bun": "^1.3.0",
    "typescript": "^5.9.2"
  }
}
