# Ekagra mobile — Supabase configuration.
# Copy to `.env` and fill with your project's values. EXPO_PUBLIC_* vars are
# inlined into the bundle by Expo, so never put a service-role key here.
EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
